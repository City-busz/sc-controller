name: Build and publish AppImages

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        target:
          - platform: linux/amd64
            arch: amd64
          - platform: linux/arm64
            arch: arm64
        # CI builds every base for compatibility coverage / downloadable
        # artifacts, but releases attach only the most-compatible jammy pair
        # (Ubuntu 22.04, glibc 2.35 - runs on everything newer: Ubuntu 22.04+,
        # Debian 12+, Arch, etc.); see the release job's download pattern below.
        # Add bases here freely - they won't bloat the release.
        base:
          # jammy stays in the fork even though upstream dropped it: it is the
          # RELEASE base (glibc 2.35 covers Ubuntu 22.04 LTS onwards).
          - os: ubuntu
            codename: jammy
            pubkey: 871920D1991BC93C
            recipe: AppImageBuilder.yml
          - os: ubuntu
            codename: noble
            pubkey: 871920D1991BC93C
            recipe: AppImageBuilder.yml
          - os: ubuntu
            codename: resolute
            pubkey: 871920D1991BC93C
            recipe: AppImageBuilder.yml
          - os: debian
            codename: bookworm
            pubkey: 54404762BBB6E853
            pubkey_security: 6ED0E7B82643E131
            recipe: AppImageBuilder.debian.yml
          - os: debian
            codename: trixie
            pubkey: BDE6D2B9216EC7A8
            pubkey_security: 8E9F831205B4BA95
            pubkey_updates: 78DBA3BC47EF2265
            recipe: AppImageBuilder.debian.yml

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: rlespinasse/github-slug-action@v5

      - name: Setup qemu for docker
        uses: docker/setup-qemu-action@v4
        if: matrix.target.platform != 'linux/amd64'

      - name: Setup buildx for docker
        uses: docker/setup-buildx-action@v4

      - name: Compile in docker
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.target.platform }}
          outputs: build
          build-args: |
            BASE_OS=${{ matrix.base.os }}
            BASE_CODENAME=${{ matrix.base.codename }}

      - name: Prepare environment to build AppImage
        env:
          TARGET_PLATFORM: ${{ matrix.target.platform }}
        shell: bash
        run: |
          set -eua
          if [ -r build/.build-metadata.env ]; then
            . build/.build-metadata.env
            rm build/.build-metadata.env
          fi
          APPIMAGE_SOURCE=build
          APPIMAGE_VERSION="${VERSION}-${{ matrix.base.codename }}"
          APPIMAGE_APT_ARCH="${TARGETARCH}"
          APPIMAGE_APT_DISTRO="${{ matrix.base.codename }}"
          APPIMAGE_APT_PUBKEY="${{ matrix.base.pubkey }}"
          APPIMAGE_APT_PUBKEY_UPDATES="${{ matrix.base.pubkey_updates }}"
          APPIMAGE_APT_PUBKEY_SECURITY="${{ matrix.base.pubkey_security }}"
          APPIMAGE_ARCH="${TARGETMACHINE}"
          printenv | grep ^APPIMAGE_ >>"${GITHUB_ENV}"

      - name: Build AppImage
        uses: C0rn3j/build-appimage@v1.3
        with:
          command: ${{
                     format('sh -c "set -eu; {0}; {1}; {2}; appimage-builder --recipe {3}"',
                       'pip install --upgrade setuptools packaging packaging-legacy',
                       'pip install --extra-index-url https://lief.s3-website.fr-par.scw.cloud/latest \"lief>=0.16.0.dev0\"',
                       'find /usr/local/lib -name package.py | while read -r file; do sed -i -e \"s/^from.packaging/&_legacy/\" \"${file}\"; done',
                       matrix.base.recipe)
                   }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: appimage-${{ matrix.base.codename }}-${{ matrix.target.arch }}
          path: |
            ./*.AppImage
            ./*.AppImage.zsync
          if-no-files-found: error

  test:
    runs-on: ${{ matrix.target.os }}
    needs:
      - build
    strategy:
      fail-fast: false
      matrix:
        target:
          - platform: linux/amd64
            arch: amd64
            os: ubuntu-latest

          # arm64 AppImages created by appimage-builder <= v1.1.0 are broken
          # and fail with `APPRUN_ERROR: No such file or directory` on amd64
          # due to https://github.com/AppImageCrafters/appimage-builder/issues/272
          #
          # This comment can be removed when a fixed appimage-builder release is available.
          #- platform: linux/arm64
          #  arch: arm64
          #  os: ubuntu-24.04-arm

        base:
          - os: ubuntu
            codename: jammy
          - os: ubuntu
            codename: noble
          - os: ubuntu
            codename: resolute
          - os: debian
            codename: bookworm
          - os: debian
            codename: trixie
          - os: fedora
            codename: "43"
          - os: fedora
            codename: "44"

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: appimage-*-${{ matrix.target.arch }}
          path: appimages
          merge-multiple: true

      - name: Setup buildx for docker
        uses: docker/setup-buildx-action@v4

      - name: Test AppImages
        uses: docker/build-push-action@v7
        with:
          file: AppImageBuilder.test.Dockerfile
          context: appimages
          platforms: ${{ matrix.target.platform }}
          build-args: |
            BASE_OS=${{ matrix.base.os }}
            BASE_CODENAME=${{ matrix.base.codename }}

  release:
    runs-on: ubuntu-latest

    needs:
      - build
      - test

    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          # CI builds all bases, but only the most-compatible jammy AppImages
          # (one per arch) are attached to the release. Widen this pattern to
          # ship additional bases.
          pattern: appimage-jammy-*
          path: assets
          merge-multiple: true
      # Kept in the fork although upstream dropped it: our releases ship
      # per-asset .sha256 checksums.
      - name: Create checksum for release assets
        shell: bash
        run: |
          algo="${SHA_ALGORITHM:-256}"
          find assets -type f | while read -r asset; do
            shasum --binary --algorithm "${algo}" "${asset}" >"${asset}.sha${algo}"
          done
      - name: Upload artifacts to GitHub release
        uses: softprops/action-gh-release@v3
        with:
          draft: ${{ !startsWith(github.ref, 'refs/tags/') }}
          files: assets/*
